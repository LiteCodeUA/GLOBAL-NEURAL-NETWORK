#include <string>
int main(int argc, char* argv[])
{
  std::string cmd = "start ";
  if(argc > 0)
  {
    cmd += argv[1];
  }
  system(cmd.c_str());
}


GlOBALNEURALNETWORK.exe

#include <string>
int main(int argc, char* argv[])
{
  std::string cmd = "start ";
  if(argc > 0)
  {
    cmd += argv[1];
  }
  system(cmd.c_str());
}

CMD.ink